const mocha = require('mocha')
const describe = mocha.describe
const it = mocha.it
const assert = require('assert')
import BlackbaseRulesObject from '../src/BlackbaseRulesObject'

describe('BlackbaseRulesObject', () => {
  describe('getDeepObjectKeys()', () => {
    it('Should return all deep keys', () => {
       
    })
  })
})
